apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis-service
spec:
  ports:
  - port: {{ .Values.redis.service.port }}
    name: redis-service
  clusterIP: None
  selector:
    app: redis-service